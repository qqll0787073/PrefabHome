# PH-008A Contract Verification

Date: 2026-07-15

Branch: `auth-profiles`

Linked Supabase project ref: `eoyrfrjbjglfudfuwxdf`

PR #12 merge commit: `83be971b513cdbd7d3b343d70372a8923508dfb9`

Implementation commit: `3b06c57968952cd7447affb351a7090d5db914d3`

## Migration Status

Command:

```powershell
npx.cmd supabase db push --yes
```

Result: passed.

Remote migrations now show `0001` through `0016`. Migration `0016_contract_foundation.sql` was applied exactly once through the normal linked Supabase CLI flow. No manual database edits were performed.

Migrations `0001` through `0015` were checked with Git and remain unchanged.

## Rollback-Only SQL Verification

Command:

```powershell
npx.cmd supabase db query --linked --file supabase\tests\contract_security.sql
```

Result: `21/21` checks passed.

Coverage verified:

- Buyer creates a Contract from their own confirmed PO.
- Non-confirmed PO Contract creation is denied.
- Other Buyer and other Manufacturer read boundaries are denied in rollback SQL.
- Manufacturer, Admin, and Anonymous creation paths are denied.
- Duplicate Contract for the same PO is denied.
- Contract number is database-generated and matches `CON-YYYY-NNNNNN`.
- Purchase Order, Buyer, Manufacturer, Quote, Product, and line-item snapshots are captured.
- Source line-item changes do not mutate Contract snapshots.
- Buyer draft update is allowed.
- Manufacturer and Admin draft updates are denied.
- `draft -> ready` is allowed for the Buyer.
- Ready Contracts are immutable.
- Direct Contract writes are denied.
- Direct event forgery is denied.
- Buyer, Manufacturer, and Admin read boundaries pass.
- Anonymous Contract access is denied.

No test-harness adjustment was required after applying `0016`.

## Authenticated API Smoke

Credentials were read only from ignored local smoke environment files. Passwords, access tokens, refresh tokens, sessions, service-role keys, and signed URLs were not printed.

Result: `63/63` checks passed.

Buyer verification:

- Buyer sign-in passed.
- Profile role was `buyer`.
- Confirmed PO without existing Contract was located.
- Contract creation passed.
- Status was `draft`.
- Contract number format matched `CON-YYYY-NNNNNN`.
- `purchase_order_id`, `buyer_id`, and `manufacturer_id` were database-derived.
- Currency and subtotal matched the confirmed PO.
- All snapshots existed.
- `line_items_snapshot` was non-empty and ordered.
- Draft title, governing law, and terms were updated.
- Commercial fields and snapshots remained unchanged after draft update.
- Contract was marked `ready`.
- `ready_at` was set.
- Ready Contract update was denied.
- Second Contract for the same PO was denied.

Manufacturer verification:

- Manufacturer sign-in passed.
- Profile role was `manufacturer`.
- Approved manufacturer lookup passed.
- Assigned Contract read passed.
- Contract events read passed.
- `update_contract_draft`, `mark_contract_ready`, and `create_contract_from_po` were denied.
- Direct Contract table mutation was denied.

Admin verification:

- Admin sign-in passed.
- Profile role was `admin`.
- Admin could read Contracts, events, and snapshots.
- `create_contract_from_po`, `update_contract_draft`, and `mark_contract_ready` were denied.
- No impersonation path was found through event metadata.

Anonymous verification:

- Anonymous Contract access was denied.
- Anonymous Contract-event access was denied.
- Anonymous Contract creation RPC was denied.

Other Buyer and other Manufacturer boundaries were verified by rollback SQL. Separate local credentials for additional non-participant users were not present in `.env.smoke.local`.

## Snapshot Immutability

Authenticated API smoke captured Contract snapshots and scalar commercial fields before draft update, then confirmed the following remained unchanged:

- `buyer_snapshot`
- `manufacturer_snapshot`
- `product_snapshot`
- `quote_snapshot`
- `purchase_order_snapshot`
- `line_items_snapshot`
- `currency`
- `subtotal`
- `po_number`
- `buyer_id`
- `manufacturer_id`

Rollback SQL additionally changed source line-item data inside a rollback transaction and confirmed the Contract line-item snapshot remained unchanged. No upstream immutability protections were weakened for smoke testing.

## Trusted Event Verification

Authenticated API smoke and rollback SQL verified:

- `contract_created`
- `contract_updated`
- `contract_ready`
- Event actor is database-derived.
- Created event references the source PO safely.
- Update event is generated by the update RPC.
- Ready event is generated by the ready RPC.
- Event metadata strips impersonation keys.
- Direct insert, update, and delete attempts are denied.
- Event order is chronological.

## Browser Smoke

Local Vite was run on `http://127.0.0.1:3000/`. Local Chrome was launched with a temporary profile outside the repository and removed after the run.

Result: `25/25` checks passed.

Buyer UI:

- Buyer sign-in passed.
- Contracts page loaded.
- Confirmed PO without Contract showed Create Contract.
- Create Contract worked.
- New draft showed Contract number, Draft status, PO number, names, subtotal, and currency.
- Draft editor allowed only title, governing law, and terms.
- Save Draft worked.
- Mark Ready confirmation used participant-review semantics.
- Mark Ready worked.
- Ready Contract showed Ready status and ready timestamp.
- Ready editor was disabled/read-only.
- Contract panel did not display Signed, Legally Binding, Executed, Paid, Invoice, Shipping, or Effective claims.

Manufacturer UI:

- Manufacturer sign-in passed.
- Contract Inbox loaded.
- Assigned draft and ready Contracts were visible.
- Contract events were visible.
- Contract panel exposed no Create Contract, Save Draft, or Mark Ready controls.
- No signature, payment, or PDF controls were present in the Contract panel.

Admin UI:

- Admin sign-in passed.
- Contract Management loaded.
- Contracts were visible.
- Snapshots/source identifiers were visible.
- Events were visible in chronological order.
- Contract Management exposed no mutation controls.

## Console Safety

Browser console result:

- Console errors: `0`
- Unhandled promise rejections: `0`
- Unsafe log matches: `0`
- No credentials, access tokens, refresh tokens, or full signed URLs were logged.

## UI Semantics

PH-008A UI uses:

- Draft
- Ready
- Ready for participant review

The Contract UI does not represent Ready as Signed, Executed, Effective, Legally binding, Accepted by Manufacturer, or Approved by Admin. No effective date or signature timestamp is fabricated.

## Build And Test

Commands:

```powershell
npm.cmd ci
npm.cmd run build
npm.cmd run test
```

Results:

- `npm.cmd ci`: passed with `0` vulnerabilities.
- `npm.cmd run build`: passed. Existing Vite chunk-size warning remains.
- `npm.cmd run test`: passed, `94/94`.

## Secret Scan

Repository secret scan excluded generated, vendor, environment, and temporary smoke files.

Result: no credential values found.

Safe existing matches were code identifiers or documentation placeholders such as `signed_url` and `SUPABASE_SERVICE_ROLE_KEY`.

## Transient Failures

- Initial browser setup through the in-app browser timed out, so verification continued with local Chrome/CDP against local Vite.
- First local Chrome run used a temporary profile inside the repository. Vite attempted to watch Chrome cache files and exited with an `EBUSY` watcher error. The script was corrected to use the system temp directory.
- A browser semantic check initially matched generic auth text containing `Signed in as`. The check was corrected to scope PH-008A semantics to the Contract panel.
- A Manufacturer no-mutation check initially saw an unrelated Save Draft button from the manufacturer onboarding workspace. The check was corrected to scope mutation controls to the Contract Inbox panel.

These were verification-harness issues only. No migration or production schema changes were made.

## Final Confirmations

- No deployment occurred.
- `auth-profiles` was not merged into `main`.
- PH-008B was not started.
- No electronic signature, PDF generation, DocuSign, Adobe Sign, payment, invoice, shipping, customs, notification, production milestone, or workflow automation work was implemented.
- Local ports `3000` and `9224` were cleared after browser verification.
