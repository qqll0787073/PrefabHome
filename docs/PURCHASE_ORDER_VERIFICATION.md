# PH-007A Purchase Order Verification

Date: July 14, 2026

Branch: `auth-profiles`

Linked Supabase project ref: `eoyfrjbjglfudfuwxdf`

## Migration Status

- PR #10 merge commit confirmed: `6c91385 Merge pull request #10 from qqll0787073/purchase-order-foundation`
- `supabase/migrations/0014_purchase_order_foundation.sql` is present.
- PR #10 did not modify migrations `0001` through `0013`.
- Applied `0014_purchase_order_foundation.sql` through the linked Supabase CLI with:

```bash
npx.cmd supabase db push --yes
```

- Remote migration list confirms `0001` through `0014` are applied.
- No manual production data edits were performed.

## SQL Verification

Command:

```bash
npx.cmd supabase db query --linked --file supabase/tests/purchase_order_security.sql
```

Result: `47/47` checks passed.

Covered:

- Buyer PO creation from own accepted Quote
- Duplicate PO denial
- non-accepted Quote denial
- missing accepted decision denial
- ownership and snapshot fields database-derived
- PO number database-generated
- Quote items copied exactly
- copied subtotal integrity
- Buyer draft update scope
- submitted PO immutability
- cancelled PO immutability
- trusted `po_created`, `po_submitted`, and `po_cancelled` events
- direct event forgery denial
- Buyer, Manufacturer, Admin, and Anonymous read/write boundaries
- timestamp authority for `submitted_at` and `cancelled_at`

The first SQL verification attempt hit a transient Supabase CLI temp-role connection error before checks ran. A retry succeeded and returned all checks passing.

## Authenticated Smoke

Local-only credentials were read from ignored `.env.smoke.local`. Passwords, access tokens, refresh tokens, and signed URL tokens were not printed.

Result: `24/24` checks passed.

Buyer:

- Login succeeded and `public.profiles.role` was `buyer`.
- Created a PO from an accepted Quote.
- Saved a draft PO.
- Submitted the PO.
- Cancelled a separate draft PO.
- Duplicate PO creation for the same accepted Quote was blocked.
- Submitted PO was read-only.
- Cancelled PO had `cancelled_at` set and `submitted_at` null.
- Submitted PO had `submitted_at` set and `cancelled_at` null.
- Timeline events were readable.
- Quote/product snapshots were present.
- Copied PO item matched the accepted Quote line item.

Manufacturer:

- Login succeeded and `public.profiles.role` was `manufacturer`.
- Assigned submitted and cancelled POs were visible.
- Mutation attempts were blocked.

Admin:

- Login succeeded and `public.profiles.role` was `admin`.
- Admin could read all smoke POs.
- Admin could read snapshots, items, and events.
- Mutation attempts were blocked.

## Browser Smoke

Local Vite was run at `http://127.0.0.1:3000/`.

Browser: local Chrome headless via Chrome DevTools Protocol.

Result: `12/12` browser checks passed.

Buyer:

- Signed in through the UI.
- Purchase Order screen loaded.
- Created a PO draft from a visible accepted Quote.
- Saved the draft.
- Submitted the PO.
- Confirmed submitted lifecycle rendering.
- Created a second PO draft.
- Cancelled the draft.
- Confirmed cancelled lifecycle rendering.

Manufacturer:

- Signed in through the UI.
- Purchase Order screen loaded.
- Assigned POs were visible.
- Purchase Order panel exposed no PO mutation controls.

Admin:

- Signed in through the UI.
- Purchase Order Management loaded.
- Snapshot rendering was visible.
- Timeline rendering was visible.
- Purchase Order Management exposed no mutation controls.

## Console Verification

- Browser console errors: `0`
- Unsafe log matches: `0`
- No credentials, access tokens, refresh tokens, or full signed URLs were observed in console output.

## Lifecycle Verification

- `draft` POs render without lifecycle timestamps.
- `submitted` POs render `Submitted ...` only.
- `cancelled` POs render `Cancelled ...` only.
- Submitted and cancelled rows remain read-only.

## Snapshot Verification

- PO snapshot fields were database-derived from the accepted Quote, Buyer profile, Manufacturer profile, and RFQ product snapshot.
- Admin browser smoke confirmed snapshot labels render in Purchase Order Management.

## Event Verification

- Trusted `po_created`, `po_submitted`, and `po_cancelled` events were generated by database RPC flows.
- Admin browser smoke confirmed timeline events render.
- Direct event insertion remained denied in SQL verification.

## Timestamp Verification

- `submit_purchase_order()` set `submitted_at` and left `cancelled_at` null.
- `cancel_purchase_order_draft()` set `cancelled_at` and left `submitted_at` null.
- Buyer, Manufacturer, and Admin timestamp forgery attempts were blocked.
- Cancelled drafts no longer fabricate submission audit data.

## Final Validation

- `npm.cmd ci`: passed; 81 packages installed, 0 vulnerabilities.
- `npm.cmd run build`: passed. Vite reported the existing chunk-size warning for the main bundle.
- `npm.cmd run test`: passed; `77/77` tests.
- Secret scan: no credential values found. Matches were existing safe placeholders or field names such as `SUPABASE_SERVICE_ROLE_KEY` and `signed_url`.

No production deployment occurred. `auth-profiles` was not merged into `main`. PH-007B was not started.
